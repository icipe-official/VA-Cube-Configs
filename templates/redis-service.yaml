apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: vectoratlas
  labels:
    io.kompose.service: redis
spec:
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
  selector:
    io.kompose.service: redis
  type: ClusterIP
